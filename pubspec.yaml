name: whatsapp_responder_app
description: Personal Android control app for WhatsApp Group Responder.
publish_to: "none"
version: 0.1.8+9

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  flutter_secure_storage: ^10.3.1
  shared_preferences: ^2.5.5
  sqflite: ^2.4.3
  path: ^1.9.1
  flutter_markdown_plus: ^1.0.12
  file_selector: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
